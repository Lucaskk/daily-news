# 2026-09-14 未產製事故

稽核日期：2026-09-15。此為系統事故紀錄，不是9/14新聞日報，不補造當日新聞。

## 已確認的時間線（Asia/Taipei）

| 時間 | 證據與結果 |
| --- | --- |
| 08:01:40 | 當日排程回合啟動；thread_turns.started_at 與凍結截點一致 |
| 08:01:57 | pipeline 建立 production-2026-09-14.json，stage=research_pending；重建歷史索引並開始搜尋 |
| 08:02:24 | 當回合 thread_items 出現 contextCompaction |
| 約08:04:58以後 | 後續操作改為讀 Codex 文件、搜尋 App／CLI 差異 |
| 08:05:32 | 回合以 completed 結束，final_agent_item 回答「Codex 的兩種操作介面」，不是新聞；error_json 沒有額度耗盡或執行失敗 |
| 10:00:39 | 唯一watchdog日誌記錄 Sent LINE missing-publish alert；這是告警，不是新聞配送 |
| 12:47:27起 | 使用者明確要求今天稍後修復、先新增LINE指令與分工。此後暫停是遵循指示，不是早上漏產原因 |

9/14日期目錄、日報與來源筆記皆未建立；checkpoint最後仍停在08:01:57，沒有commit／Pages／LINE新聞成功階段。因此直接原因是上下文整理後轉答歷史問題並提前收尾，不是先有完成的新聞卻被LINE漏送。

## 防護缺口與不確定性

- guard-2026-09-14.json 的接續次數為0，後來因 new_user_input 暫停；檔案沒有當時每個Hook的時間線，不能由最後paused倒推08:05已暫停。
- 2026-09-15唯讀 hooks/list 顯示 SessionStart、UserPromptSubmit、Stop、Interrupt 均 enabled/trusted，無解析錯誤。這證明目前配置狀態，不證明9/14每一事件都有被桌面宿主呼叫。未修改信任紀錄。
- 現有程式遇到Stop缺少turn_id會直接略過；對同一turn_id則一直重複先前block結果，没有把stop_hook_active表示的真正再次接續算入上限。這兩個是本次可重現的程式缺口，但現存歷史不足以斷言它們就是9/14未攔截的唯一原因。
- Python單元測試通過不等於已验证Codex桌面實際compact／Stop生命週期。後續需以真正排程的Hook事件紀錄與完成檢查判定。

## 本次修正

1. 在精確專案／對話與active防護範圍內，Stop缺少turn_id時仍可做完成檢查；新使用者指令或Interrupt依然優先暫停。
2. 同一回合的stop_hook_active再次接續也計入既有次數／時間／無進展限制，不新增排程、不重設預算。
3. 私人guard-events-YYYY-MM-DD.json保存最近64個Hook事件的時間、類型、是否帶turn_id與當時狀態；不存聊天正文或密鑰。
4. watchdog若確認research_pending且日報／來源筆記均不存在，明報「研究尚未落檔、未進入發布」，不把未知問題猜成額度耗盡。
5. 新增可退回legacy的Python來源快取／歷史查詢工具，並讓pipeline與恢復提示附目前模式。新版預設不啟用，先保留原本輸出流程。

9/14仍未補產或重送；不把今天查到的新事件包裝成昨日新聞，不改寫成功狀態。發生在宿主沒有呼叫Hook、模型額度耗盡、程式關閉或電腦離線時，這些修正仍不能保證自行恢復。

官方事件欄位與Stop行為依據：[OpenAI Hooks文件](https://learn.chatgpt.com/docs/hooks)。
